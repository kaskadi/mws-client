{
  "name": "mws-sdk",
  "version": "0.0.1",
  "description": "an api wrapper for Amazon Marketplace Webservice API",
  "main": "index.js",
  "scripts": {
    "test": "standard --fix && nyc --reporter=lcov mocha",
    "coverage": "nyc --reporter=html mocha && xdg-open ./coverage/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.1.0",
    "node-fetch": "^2.6.0",
    "xml2json": "^0.11.2"
  },
  "devDependencies": {
    "mocha": "^6.2.0",
    "nyc": "^14.1.1"
  }
}
